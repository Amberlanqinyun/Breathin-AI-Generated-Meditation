from flask import Flask

app = Flask(__name__)
app.secret_key = 'your_secret_key'
app.config['SESSION_TYPE'] = 'filesystem'

# Import error handling and views after initializing the app to avoid circular imports
import app_error_handling
import app_views

if __name__ == '__main__':
    app.run(debug=True)


